# Python 의존성 — 검증/스모크 스크립트(scripts/) 실행용
# 설치: python -m pip install -r requirements.txt
# 이후 1회: python -m playwright install chromium  (runtime-gate 브라우저 회귀용)
jsonschema>=4.18
PyYAML>=6.0
playwright>=1.40
Pillow>=10.0
# 하천 공간자료 전처리(scripts/build_*_layers.py, extract_river_layers.py) 전용.
# 검증 게이트에는 쓰이지 않으므로 스모크만 돌릴 때는 없어도 된다.
numpy>=1.26
pyproj>=3.6
pyshp>=2.3
