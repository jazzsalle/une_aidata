# 32. 소하천 전체 목록(NDMS) ↔ 소하천구역 SHP 매핑 기준

작성 2026-08-18 · 데이터 구축 담당자에게 그대로 넘기는 문서다.
산출 스크립트 `scripts/build_sochun_ledger_crosscheck.py` (`npm run data:sochun-crosscheck`).

---

## 1. 왜 필요한가

지도에 그려지는 것은 SHP 이고, 소하천이 무엇인지 규정하는 원장은 NDMS 목록이다. 두 자료에
**공통 식별자가 없다.** 그래서 "지도에 표시되는 소하천이 전체 목록을 얼마나 덮는가"를 답하려면
조인 기준을 먼저 정해야 한다.

| 자료 | 출처 | 규모 |
|---|---|---:|
| 소하천 전체 목록 | 행안부 NDMS 사업단 → `소하천 전체 목록/소하천대장_20260814_좌표추가.xlsx` | 22,465행 (시군구+명칭 고유 22,144) |
| 소하천구역 연속주제도 | 국토교통부 / 브이월드 `LSMD_CONT_UJ301_{시도}.zip` 17개 | 116,758 폴리곤 |

**단위가 다르다.** 116,758은 하천 수가 아니라 도형 조각 수다 — 소하천구역은 고시 구간·좌우안·
예정지 단위로 쪼개져 오며 하천 1개당 평균 5.67, 중앙값 3조각이다. 이름으로 묶으면 19,684개다.
따라서 **개소 기준의 정본은 NDMS 목록**이고 SHP 는 그 부분집합으로 본다.

---

## 2. 조인 키

```
COL_ADM_SE (5자리 행정표준코드)  +  정규화 소하천명
```

- `COL_ADM_SE` 는 전국 188개 값이 전부 5자리 시군구코드다 (47190 = 구미시).
- NDMS 목록에는 코드가 없고 `시도`·`시군구` 문자열만 있다 → **시군구 코드표가 조인의 전제**다 (§5).
- 정규화는 **공백 제거만** 한다. `구기1천`과 `구기천`은 서로 다른 하천이므로 접미 숫자를 건드리지 않는다.

### 하천명이 들어 있는 곳

원자료 DBF 필드는 6개(`ALIAS·REMARK·NTFDATE·SGG_OID·COL_ADM_SE·MNUM`)이고
**소하천명 전용 필드도 소하천 코드도 없다.** 이름은 자유기술 칸에 섞여 온다.

| 이름이 있는 칸 | 폴리곤 | 비율 |
|---|---:|---:|
| `REMARK` 에만 | 72,819 | 62.4% |
| 둘 다 | 33,913 | 29.0% |
| `ALIAS` 에만 | 4,776 | 4.1% |
| 없음 | 5,250 | 4.5% |

**`ALIAS` 와 `REMARK` 중 하나에만 있어도 보유로 본다.** `ALIAS` 는 `소하천예정지`(47,768건)·
`소하천구역`(10,549건)·빈칸(10,094건)이 대부분이라 그것만 보면 3분의 2를 잃는다.
`ALIAS` 를 먼저 보고 실패하면 `REMARK` 를 본다.

### 이름 추출 규칙

`scripts/build_sochun_layers.py` 의 `stream_name_of()` 가 정본이다. 순서대로:

1. 괄호 안 고시 일련번호 제거 — `운수천(001-2)` → `운수천`
2. 선두 고시번호 제거 — `3-01 소재골천` → `소재골천`
3. `예정지\s*\d*` 제거 — `큰터골천 예정지03` → `큰터골천`
4. `소하천정비법_` 제거
5. `( ) [ ] _ / , 공백` 으로 분해
6. 조각 중 `[가-힣A-Za-z0-9]{1,12}(천|강|川)` 에 **완전일치**하고 제외 목록에 없는 것 → 가장 긴 것
7. 없으면 원문 전체에서 같은 형태를 `search`
8. 그래도 없으면 **빈 문자열** — 없는 이름을 만들지 않는다

제외 목록:

| 종류 | 값 | 근거 |
|---|---|---|
| 구분 라벨 (`GENERIC`) | 소하천구역 · 소하천예정지 · 소하천 · 구역 · 예정지 | 하천명이 아니다 |
| 비하천 라벨 (`BLOCK`) | 하천 · 폐천 · 기존폐천 · 신생폐천 | '천'으로 끝나지만 폐지 구역 라벨. 포천시 `기존폐천` 1,383건 등 |

**`구하천`은 제외하지 않는다** — 전북 완주군·남원시에 실제로 등재된 소하천명이다.
제외 목록에는 **NDMS 목록에 같은 이름이 등재돼 있지 않은 것만** 넣는다.

이 규칙으로 전국 116,758건 중 **111,508건(95.5%)** 에서 이름을 얻는다(종전 규칙 72.9%).
게이트 `scripts/smoke_river_reference_layers.py` 의 `GENERIC_NAMES` 가 같은 목록을 검사한다.

**알려진 잔여 오검출 1건.** 인천 부평구 폴리곤의 `REMARK` 가 `<인천광역시 부평구 고시 제2020-13호>`
인데 여기서 마지막 단계의 `search` 가 `인천` 을 하천명으로 집어낸다. 고시문 문구에서 이름 형태가
잘려 나온 경우다. 이를 막는 규칙(찾은 이름 뒤에 한글이 이어지면 거부)을 전수 실측해 보면
`궁지천소하쳔예정지`(원문 오타) 처럼 하천명과 구분이 붙어 오는 **정상 표기 173건을 잃는다.**
1건을 막으려고 173건을 버리는 교환이라 **파서를 고치지 않고 이 자리에 적어 둔다.**

---

## 3. 쓰면 안 되는 것

| 후보 | 왜 안 되는가 |
|---|---|
| `MNUM` | 고시 관리번호다. 33자리지만 앞 20자(고시 단위) 고유값이 전국 1,176개뿐이라 하천 식별자가 아니다 |
| NDMS 좌표 `PNTTM_XCRD/YCRD` | 22,465행 중 시점 4,488행(20%)·종점 72행(0.3%)뿐이고 **대상 6개 지역은 전부 0행**. 시도별 최적 원점을 잡아도 중앙오차 30~290 km, `-9.9`·`35,183,382` 같은 값이 섞여 있다 |
| NDMS 주소 | 462행이 개편 전 지명(청원군→청주시, 당진군→당진시, 여주군→여주시), `강원특별자치도 부천시 오정구` 같은 오입력 6행 |
| 시군구를 넘는 명칭 일치 | §4 참조 |

---

## 4. 시군구를 넘는 명칭 일치는 보유로 치지 않는다

"소하천이 여러 지자체에 걸쳐 있으니 다른 시군구에 같은 이름이 있으면 형상이 있는 것으로 보자"는
판정은 **성립하지 않는다.** 실측했다.

- 전국 SHP 고유 하천명 12,477종 중 **2,595종이 2개 이상 시군구**에 같은 이름으로 존재한다
  (절골천 66곳, 큰골천 57곳, 신기천 52곳).
- NDMS 목록 쪽도 2,043건이 같은 시도 내 복수 시군구에 등재돼 있다.
- 코드가 다른 동명 형상까지의 거리를 950건에 대해 재면
  **중앙값 26.2 km · p10 5.6 km · p90 74.4 km**, 3 km 이내는 3.6%뿐이다.
- NDMS 목록의 시점·종점 주소가 실제로 다른 시군구인 행은 6건이고 **전부 오입력**이다.

→ **판정: 시군구코드가 다르면 `미보유`.** 단 3 km 이내인 것만 `경계걸침_후보` 로 갈라
   사람이 본다. 전국 **25건**이며 전량이 `build/sochun/crosscheck_rows.csv` 에 있다.

같은 시군구 안에서도 동명이 303건 있다(경북 영주시 갓골천 2건 등). 이런 행에는
`homonym_warning=Y` 를 달아 자동 조인 결과를 그대로 신뢰하지 않게 한다(전국 5,508행).

---

## 5. 시군구 코드표 — `data/reference/sgg_code_map.json`

`npm run data:sgg-code-map` → `scripts/build_sgg_code_map.py` 가 만든다.
원자료는 **행정표준코드시스템 법정동코드**(브이월드 배포, 2026-08-13 갱신) 하나다.

```
행정구역/행정표준코드시스템_법정동 코드(브이월드)_260813갱신/LSCT_LAWDCD.zip
  LAWD_CD(10자리, 앞 5자리 = 시군구코드) · SIDO_NM · SGG_NM · UMD_NM · RI_NM
  CRE_DT(생성일) · DEL_DT(폐지일) · OLD_LAWDCD(종전 코드)
  31,172행 = 유효 20,552 + 폐지 10,620
```

`DEL_DT` 와 `OLD_LAWDCD` 가 들어 있어서 **개편 이력이 원자료 안에서 풀린다.**
이름 대조나 공간판정 같은 추정 수단을 쓰지 않는다.

맞춰야 하는 것이 셋이다.

**① 층위** — `SGG_NM` 은 자치구가 있는 시에서 구 단위다(`창원시 성산구` 48123).
반면 SHP 의 `COL_ADM_SE` 는 **시 단위**를 쓴다(창원시 48120, 청주시 43110, 용인시 41460 —
188개 중 181개가 끝자리 0). 구 코드 앞 4자리 + `0` 을 **시 단위 rollup** 으로 함께 넣는다.
그래서 `entries` 의 `codes` 는 배열이다.

**② 개편 시차** — 원자료마다 기준일이 다르다. 2026-06-30 개편에서

- 전남·광주가 **전남광주통합특별시**(시도코드 `12`)로 통합됐다. 종전 `29xxx`(광주)·`46xxx`(전남)는
  전부 폐지됐는데, **소하천구역 SHP 는 아직 종전 코드를 쓴다**(SHP 의 23개 코드가 여기 해당).
- 인천에 **영종구(28155) · 제물포구(28125) · 서해구(28275) · 검단구(28290)** 가 생겼다.
  이쪽은 반대로 **SHP 가 이미 신설 코드를 쓴다**(97 폴리곤).

`OLD_LAWDCD` 로 종전 코드를 같은 시군구의 별칭으로 함께 담아 양쪽을 다 받는다.
예: 전남광주통합특별시 목포시 → `["12110", "46110"]`.
지금도 다른 시군구가 쓰고 있는 코드는 별칭으로 담지 않는다.

**③ 승계가 갈리는 코드** — 종전 인천 중구(28110)는 영종구와 제물포구로, 서구(28260)는
검단구와 서해구로 갈라졌다. 코드만으로는 어느 쪽인지 정할 수 없으므로 **표에 넣지 않고**
`ambiguous_old_codes` 에 근거와 함께 남긴다. 현재 소하천구역 SHP 는 이 두 코드를 쓰지 않는다.

```jsonc
{"dataset": "sgg_code_map", "version": "2.0",
 "ambiguous_old_codes": [{"code": "28260", "claimed_by": ["인천광역시 검단구", "인천광역시 서해구"],
                          "reason": "한 시군구가 둘로 갈려 종전 코드의 승계가 정해지지 않는다."}],
 "entries": [{"sido": "전남광주통합특별시", "sgg": "목포시", "codes": ["12110", "46110"]}]}
```

**결과: 시군구 항목 288개.**

- 소하천구역 SHP 의 `COL_ADM_SE` **188개가 전부** 이 표로 풀린다.
- NDMS 목록의 시군구 199개 중 **198개**가 풀린다. 남는 하나는 **제주 42행**으로, 시군구 칸과
  주소 칸이 모두 비어 있어 어느 쪽으로도 풀 수 없다. 추정하지 않고 `코드미상` 으로 둔다.

> 이 자료를 받기 전에는 하천명 중첩으로 파생표를 만들어 썼다(199개 중 170개 성립,
> `build/sochun/sgg_code_map_derived.json`). 정본이 있으면 그쪽이 우선하며, 파생표 경로는
> 원자료가 없는 환경을 위해 남겨 둔다.

---

## 6. 대조표 산출물

`npm run data:sochun-crosscheck` → `build/sochun/`. 검증용이며 앱에 반입하지 않는다.

| 파일 | 내용 |
|---|---|
| `crosscheck_rows.csv` | NDMS 목록 한 행 = 대조표 한 행 (22,465행) |
| `crosscheck_summary.csv` | 시군구별 요약 |
| `unmatched_shp.csv` | 역방향 — 목록에 닿지 못한 SHP 쪽 (읽는 법은 아래) |
| `crosscheck_report.md` | **이 절의 수치 표를 생성한다** |
| `sgg_code_map_derived.json` | 정본 코드표가 없을 때 쓴 파생표 |

> 아래 표들은 `crosscheck_report.md` 에서 옮긴 것이다. 원자료가 갱신되면
> `npm run data:sochun-crosscheck` 를 돌려 그 파일과 대조해 고친다 — 손으로 센 값이 아니다.

`crosscheck_rows.csv` 열: `sido · sgg · sgg_code · stream_name · stream_name_norm ·
sugye · length_m · basin_km2 · start_addr · shp_status · shp_polygon_count ·
shp_mnum_list · shp_name_source · homonym_warning · match_basis`

### 판정 결과 (2026-08-18 실측)

| 상태 | 행 | 비율 | 뜻 |
|---|---:|---:|---|
| `보유` | 17,613 | 78.4% | 시군구코드·명칭 모두 일치 |
| `경계걸침_후보` | 28 | 0.1% | 코드는 다르나 동명 형상이 3 km 이내 — 사람이 확인 |
| `미보유` | 4,782 | 21.3% | 형상 없음 |
| `코드미상` | 42 | 0.2% | 제주 — 시군구 칸도 주소 칸도 비어 있다 |

`보유` 행의 이름 출처: `REMARK` 10,667 · `BOTH` 5,416 · `ALIAS` 1,530
— **`ALIAS` 만 읽었다면 보유의 61%(10,667행)를 놓쳤을 것이다.**

| 시도 | 목록 | 보유 | 보유율 | 미보유 | 코드미상 |
|---|---:|---:|---:|---:|---:|
| 강원특별자치도 | 2,510 | 2,163 | 86.2% | 343 | 0 |
| 경상북도 | 3,656 | 3,132 | 85.7% | 521 | 0 |
| 전북특별자치도 | 2,455 | 2,024 | 82.4% | 428 | 0 |
| 충청남도 | 2,159 | 1,700 | 78.7% | 456 | 0 |
| 경상남도 | 2,998 | 2,283 | 76.2% | 709 | 0 |
| 세종특별자치시 | 100 | 76 | 76.0% | 24 | 0 |
| 경기도 | 2,103 | 1,574 | 74.8% | 525 | 0 |
| 전남광주통합특별시 | 3,711 | 2,751 | 74.1% | 956 | 0 |
| 충청북도 | 2,039 | 1,500 | 73.6% | 538 | 0 |
| 대구광역시 | 220 | 210 | 95.5% | 10 | 0 |
| 대전광역시 | 87 | 50 | 57.5% | 37 | 0 |
| 울산광역시 | 136 | 69 | 50.7% | 67 | 0 |
| 부산광역시 | 32 | 14 | 43.8% | 18 | 0 |
| 인천광역시 | 112 | 40 | 35.7% | 72 | 0 |
| 제주특별자치도 | 123 | 26 | 21.1% | 55 | 42 |
| 서울특별시 | 24 | 1 | 4.2% | 23 | 0 |

제주를 뺀 모든 시도에서 `코드미상` 이 0 이다 — §5 의 정본 코드표를 넣은 효과다. 종전 파생표에서
`코드미상` 이던 787행이 코드를 얻어 판정되었고, 그 787행이 전부 `미보유` 로 확정되면서 미보유
열이 그만큼 늘었다. 원인은 아래 소절에 있다.

**서울은 SHP 22건 중 이름이 읽히는 것이 진관천·학암천 2건뿐**이라 지도 표출은 되어도
검색으로 찾아갈 수 없다. 광역시 전반이 낮은 것도 같은 이유다.

### 미보유 4,782행은 원인이 둘로 갈린다

| 원인 | 행 | 뜻 |
|---|---:|---|
| 해당 시군구 SHP 에 **이름 있는 하천이 0종** | 535 | 지자체가 소하천구역에 하천명을 아예 안 넣었다 |
| 이름이 목록 개소의 20% 미만인 시군구 | 805 | 같은 원인의 정도 차이 |
| 그 외 — 자료는 있는데 이 하천만 없다 | 3,442 | 미고시·미정비 또는 연속주제도 갱신 지연 |

앞의 두 줄(1,340행, 미보유의 28%)은 **형상이 없는 게 아니라 이름이 없는 것**이라 성격이 다르다.
실제로 확인한 예:

| 시군구 | SHP 폴리곤 | 이름 있는 하천 | 목록 개소 | 들어 있는 이름 |
|---|---:|---:|---:|---|
| 경기 용인시 | 154 | 6 | 149 | 경안천·복하천·진위천·청미천·탄천·한천 — 전부 **본류명**이다 |
| 경남 김해시 | 25 | 1 | 106 | 율하뜰소하천 |
| 강원 속초시 | 44 | 0 | 36 | — |
| 전남 순천시 | — | 0 | 252 | — |

용인시는 소하천구역 폴리곤에 소하천명 대신 합류하는 본류 이름을 적어 두었다.
**파서로 풀 수 있는 문제가 아니라 원자료 보완이 필요한 자리다.**

| 그 밖의 결손 | 규모 |
|---|---:|
| 이름 없는 폴리곤 | 5,250건 (전국 폴리곤의 4.5%) |
| SHP 에만 있는 하천명 | 2,352종 · 폴리곤 13,427건 |

### `unmatched_shp.csv` 를 읽는 법

**기준 목록은 NDMS 소하천 전체 목록이고, 판정 단위는 `시군구 + 하천명` 조합이다.**
전국 단위가 아니다 — 같은 이름이 **다른 시군구** 목록에 있어도 여기 실린다.

한 파일에 성격이 다른 두 종류가 들어 있다.

| `kind` | 행 | 폴리곤 | 뜻 |
|---|---:|---:|---|
| `목록에 없는 하천명` | 2,352 | 13,427 | `(시군구코드, 하천명)` 조합이 SHP 에 있는데, 그 시군구의 목록 행 중 `보유` 로 판정된 것이 하나도 없다 |
| `이름 없는 폴리곤` | 107 (시군구코드 단위) | 5,250 | `ALIAS`·`REMARK` 어디서도 이름을 못 뽑아 **조인 대상이 되지 못한** 형상. 목록과 무관하다 |

`이름 없는 폴리곤` 행은 `stream_name` 칸이 비고 `polygon_count` 만 있다. 시군구코드별 합계다.

읽을 때 주의할 것 둘.

- **`경계걸침_후보` 28건의 상대 폴리곤도 `목록에 없는 하천명` 으로 남는다.** 그 판정 자체가
  "이 시군구 목록에는 없고 옆 시군구 형상이 3 km 안에 있다"는 뜻이므로 모순은 아니다.
  두 CSV 를 겹쳐 볼 때 같은 하천명이 양쪽에 보이는 이유가 이것이다.
- 목록 쪽 행이 하나도 없는 시군구는 `sido`·`sgg` 칸이 빈다(현재 인천 부평구 1행).

---

## 7. 하천 자료를 어떻게 나눠 쓰는가 (2026-08-19 개정)

> 이 절을 외부(T3Q)에 보낼 수 있게 정리한 것이 `docs/33_river_dataset_linkage.md` 다.
> 국가기본도 중심선의 하천코드가 하천일람·하천망도와 같은 체계라는 대조 결과가 거기 있다.

자료 4종의 역할이 겹치지 않게 나눴다. **형상을 반입하는 것은 면(폴리곤)뿐이다.**

| 자료 | 규모 | 형상 반입 | 무엇을 주나 |
|---|---:|---|---|
| 소하천구역 `LSMD_CONT_UJ301` | 116,758 | **○** 시군구 188개 | 소하천 면 + 이름(95.5%) |
| 국가기본도 하천경계 `TN_RIVER_BNDRY` | 140,851 | ○ (전국 확대 예정) | 면만. 이름·등급·코드 **없음** |
| 국가기본도 실폭 `TN_RIVER_BT` | 28,262 | ○ (전국 확대 예정) | 면만. 이름·등급·코드 **없음** |
| 국가기본도 중심선 `TN_RIVER_CTLN` | 3,224,769 | **×** | 등급·이름·코드 (전처리 조인 전용) |
| 하천망도 (국가·지방) | 3,856 | **×** | 코드·이름·등급 + 라벨점 (카탈로그) |

### 형상을 반입하지 않는다 — 정확한 뜻

**선·면 지오메트리를 클라이언트에 싣지 않는다**는 뜻이다. 하천당 **표기용 파생점 1개**는
예외이며 파생임을 함께 남긴다(`label_point_kind`). 이 구분을 적어 두지 않았다가 라벨 레이어를
과잉 삭제해 지도에서 하천명이 통째로 사라진 적이 있다(754fbfb → 3246ce6 에서 복구).

### 왜 중심선이 등급 조인 원천인가

하천망도로 조인해 보고 이미 반입된 3개 지역의 중심선 판정과 대조했다.

| 지역 | 하천망도가 판정한 건 | 중심선과 일치 | 정확도 |
|---|---:|---:|---:|
| 의왕 하천경계 | 187 | 127 | 68% |
| 구미 하천경계 | 721 | 563 | 78% |
| 남원 하천경계 | 1,095 | 824 | 75% |

틀리는 방향이 나쁘다 — **소하천을 지방·국가하천으로 올린 것**이 의왕 46 · 구미 158 · 남원
205건이다. 하천망도의 법정 하천구역 폴리곤이 넓어 본류에 합류하는 소하천 구간까지 삼킨다.
중심선은 경계·실폭과 **같은 국가기본도에서 나온 짝**이라(같은 측량, 같은 2024-04-08 갱신)
`RIVER_SE` 가 원자료 속성이고 3개 지역 등급미확인이 1% 미만이다.

중심선 전수 구성:

```
RVC005 세류    2,726,250 (84.5%)  이름  0.1%
RVC003 소하천    314,942 ( 9.8%)  이름 10.6%
RVC002 지방하천  164,621 ( 5.1%)  이름  100%   코드도 100%
RVC001 국가하천   14,653 ( 0.5%)  이름  100%   코드도 100%
RVC004 기타하천    4,303          이름  0.1%
```

### 하천명은 어떻게 표출하는가

| 등급 | 라벨 좌표 | 만드는 곳 |
|---|---|---|
| 국가·지방하천 | `river_network_catalog.json` 의 `label_point` | 전처리 — 하천망도 최대 링의 **내부점** |
| 소하천 | 폴리곤 레이어가 직접 | 프런트 — 이름별 최대 조각의 `getInteriorPoint()` |

**bbox 중심은 쓰지 않는다.** `nav` 는 카메라 목표점이라 화면에 그려지지 않지만 라벨은 그 좌표에
글자가 찍혀 '이 하천이 여기다'를 눈으로 주장한다. 굽은 하천의 bbox 중심은 옆 하천 위에 떨어져
**A천 위에 B천 이름이 찍힌다.** 실제로 한강은 nav `127.466,37.345` → label `127.861,37.039` 로
물길 위로 옮겨졌다.

전국 3,856개를 한꺼번에 찍으면 지도가 글자로 덮이므로 등급별 해상도 상한을 둔다 —
국가하천 160 · 지방하천 45 · 소하천 10 m/px 부터 보이고, 라벨 레이어는 declutter 로 겹치는
글자를 감춘다.

### 국가·지방하천의 시군구 배정

하천망도에 행정구역 정보가 없어 **행정동 경계와의 공간판정**으로 정한다
(`npm run data:river-network`). **하천을 자르지 않는다** — 여러 시군구를 지나면 그 시군구를
전부 `admin_codes` 에 적는다. 형상을 자르면 원자료에 없는 경계선을 만들게 되고, 목록에는
'이 시군구에서 볼 수 있는 하천'이면 충분하다.

결과: 3,856 하천 전부 배정(미배정 0). 1개 시군구 3,286 · 2개 463 · 3개 이상 107(최대 19 —
한강이 서울 7개구·경기 8개시·충북 3개·강원 1개를 지난다).

### 소하천구역과 하천경계는 중복이 아니다

소하천구역 폴리곤의 표본점이 상대 폴리곤 안에 들어가는지로 쟀다.

| 지역 | 하천경계와 겹침 | 실폭과 겹침 |
|---|---:|---:|
| 의왕 | 27/110 (25%) | 18/110 (16%) |
| 구미 | 78/159 (49%) | 59/159 (37%) |
| 남원 | 122/454 (27%) | 78/454 (17%) |

표본점 겹침률이 6~30%에 그쳐 같은 도형을 두 번 그린 것이 아니다. 소하천구역은 소하천정비법상
**고시 구역**(예정지 포함)이고 하천경계는 국가기본도의 **측량 현황**이다. 삭제 대상이 아니다.

---

## 8. 상태 표기 규칙

이 대조 결과로 만들어지는 속성은 실제 자료가 아니라 파생값이다. CLAUDE.md 규칙 4에 따라
다음을 함께 남긴다.

```jsonc
{"official_data": false, "value_status": "derived",
 "join_basis": "시군구코드+명칭", "homonym_warning": true}
```

`보유` 판정을 "형상이 실제로 그 하천의 것임이 확인됐다"는 뜻으로 쓰지 않는다.
**명칭 일치 검증이지 위치 검증이 아니다** — 위치를 검증하려면 NDMS 좌표가 필요한데
대상지역에는 한 건도 없다(§3).

---

## 9. 남은 것

1. **`경계걸침_후보` 28건 육안 확인** — QGIS 로 열어 실제 경계 걸침인지 동명이인인지 가른다.
   경기 군포시 송정1천은 거리가 0.0 km 라 실제 걸침일 가능성이 높다.
2. **하천명이 안 들어온 시군구의 원자료 보완** — 순천시(목록 252 / SHP 이름 0)·
   용인시(149 / 6)·김해시(106 / 1)·속초시(36 / 0) 등. 지자체 고시문 대조가 필요하며
   추정으로 채우지 않는다. 미보유 4,782행 중 1,340행이 여기 해당한다.
3. **제주 42행** — NDMS 목록의 시군구·주소가 비어 있다. NDMS 사업단에 보완 요청 대상.
   코드표로는 풀 수 없는 유일한 자리다.
4. **이름 없는 폴리곤 5,250건** — 위 2번과 같은 성격이다.
5. 소하천 표출 범위를 대상 6개 지역 밖으로 넓히려면 색인 분할 로딩이 선행되어야 한다
   (전국 116,758 폴리곤 ≈ 150 MB).
