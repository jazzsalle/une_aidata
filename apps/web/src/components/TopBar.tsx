import type {CurrentSituation} from '../types/contracts';
interface Props{situations:CurrentSituation[];selected:CurrentSituation|null;onSelect(id:string):void;onSave():void;}
export function TopBar({situations,selected,onSelect,onSave}:Props){return <header className="topbar"><div className="brand-block"><strong>재난안전 AI 대응지원</strong><span>POC · 현재조건+지식데이터</span></div><label><span>지역</span><select value={selected?.situation_id??''} onChange={e=>onSelect(e.target.value)}>{situations.map(i=><option key={i.situation_id} value={i.situation_id}>{i.admin_name}</option>)}</select></label><div className="top-field"><span>기준시각</span><strong>{selected?new Date(selected.reference_time).toLocaleString('ko-KR'):'-'}</strong></div><div className="top-field"><span>모드</span><strong>{selected?.mode==='scenario'?'시나리오':selected?.mode==='hybrid'?'공공 API+입력':'실시간'}</strong></div><div className="top-field"><span>재난유형</span><strong>{selected?.hazards.join(' · ')??'-'}</strong></div><div className="top-field"><span>사용자모드</span><strong>공공</strong></div><button type="button" className="primary ghost" onClick={onSave}>상황뷰 저장</button></header>}
